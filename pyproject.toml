[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas-utils"
version = "0.1.0"
dependencies = [
    "unidecode",
    "boto3",
    "s3fs"
]

[tool.setuptools.packages.find]
include = ["utils*", "PipelinesConsumo*", "src*"]